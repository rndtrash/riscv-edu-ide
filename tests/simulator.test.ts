import { describe, it, expect } from 'vitest';

describe('simulator', () => {
    it('runs with default setup', () => {
        expect(true).toBe(true);
    });
    it('can tick', () => {
        expect(true).toBe(true);
    });
    it('can load ROM from file', () => {
        expect(true).toBe(true);
    });
});